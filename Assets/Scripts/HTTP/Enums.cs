using UnityEngine;
// какие то енамы
public enum Enum_HTTPMethods // методы
{
    NULL = 0,
    GET = 1,
    POST = 2,
    PUT = 3,
    DELETE = 4,
}

public enum Enum_ContentType //тип контента
{
    NULL,
    HTML,
    JSON,
    TEXT,
    XML,
    JPEG,
    PNG,
    OCTET_STREAM
}

