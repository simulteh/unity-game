using UnityEngine;
// какие то енамы
public enum Enum_HTTPMethods // методы
{
    GET,
    POST,
    PUT,
    DELETE,
}

public enum Enum_ContentType //тип контента
{
    HTML,
    JSON,
    TEXT,
    XML,
    JPEG,
    PNG,
    OCTET_STREAM
}

